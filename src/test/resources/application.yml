reverse-proxy:
  filter-order: 2147483647
  timeout:
    connect: 500
    read: 2000
  mappings:
    -
      path: /uri/1
      hosts: http://localhost:8080, localhost:8081
      strip-path: true
    -
      path: /uri/2
      hosts: http://localhost:8080, localhost:8081
      strip-path: false
